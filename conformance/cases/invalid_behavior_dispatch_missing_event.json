{
  "version": "hsm-conformance-v1",
  "name": "invalid_behavior_dispatch_missing_event",
  "mode": "validation",
  "description": "dispatch behavior ops must declare the event to dispatch.",
  "features": ["validation"],
  "model": {
    "name": "InvalidBehaviorDispatchMissingEvent",
    "initial": "idle",
    "states": [{"name": "idle"}]
  },
  "behaviors": {
    "bad": [{"op": "dispatch"}]
  },
  "expect": {
    "validation": [{"code": "invalid_behavior_op_operand"}]
  }
}
