{
  "version": "hsm-conformance-v1",
  "name": "invalid_behavior_event_metadata_set_missing_value",
  "mode": "validation",
  "description": "event_metadata_set behavior ops must declare the value to write.",
  "features": ["validation"],
  "model": {
    "name": "InvalidBehaviorEventMetadataSetMissingValue",
    "initial": "idle",
    "states": [{"name": "idle"}]
  },
  "behaviors": {
    "bad": [{"op": "event_metadata_set", "name": "owner"}]
  },
  "expect": {
    "validation": [{"code": "invalid_behavior_op_operand"}]
  }
}
