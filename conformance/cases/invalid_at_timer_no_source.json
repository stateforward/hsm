{
  "version": "hsm-conformance-v1",
  "name": "invalid_at_timer_no_source",
  "mode": "validation",
  "description": "An at timer trigger must declare exactly one timepoint source.",
  "features": ["timer", "at", "validation"],
  "model": {
    "name": "InvalidAtTimerNoSource",
    "initial": "waiting",
    "states": [
      {
        "name": "waiting",
        "transitions": [
          {
            "trigger": {
              "kind": "at"
            },
            "target": "done"
          }
        ]
      },
      {
        "name": "done"
      }
    ]
  },
  "expect": {
    "validation": [
      {
        "code": "invalid_timer_source"
      }
    ]
  }
}
